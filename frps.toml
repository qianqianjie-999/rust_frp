# 服务器配置文件

# 服务器基本配置
bind_addr = "0.0.0.0"
bind_port = 7000

# 禁用服务器端 Web 管理界面
[web_server]
port = 0

# 认证配置
[auth]
method = "token"
token = "test_token"

# 传输配置
[transport]
protocol = "tcp"
tls = { enable = true }  # 使用内置证书
